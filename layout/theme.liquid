<!DOCTYPE html>
<html lang="{{ shop.locale }}">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{{ page_title }}</title>
  <meta name="description" content="Discover the latest smart home devices for automation, security, and energy savings. Shop thermostats, cameras, lights, and more with fast shipping.">
  {{ content_for_header }}
  <link rel="stylesheet" href="{{ 'base.css' | asset_url }}">
  <script src="{{ 'theme.js' | asset_url }}" defer></script>
</head>
<body class="bg-gray-50 text-gray-900">
  {{ content_for_layout }}
</body>
</html>